run:
  dataset: maros_meszaros
  output_dir: results
  parallelism: 4
  resume: true
  timeout_seconds: 100

solvers:
  - id: scs_baseline
    solver: scs
    settings:
      eps_abs: 1.0e-6
      eps_rel: 1.0e-6
      max_iters: 1000000
